#ignore the git local files and the c9 project files
.git
.c9

# ignore the repositories where webpack puts the bundles
dist/
public/bundle/*
node_modules/

#ignore these to see if I can push now
.github/
public/